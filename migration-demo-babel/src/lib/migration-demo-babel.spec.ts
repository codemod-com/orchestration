import { migrationDemoBabel } from './migration-demo-babel';

describe('migrationDemoBabel', () => {
  it('should work', () => {
    expect(migrationDemoBabel()).toEqual('migration-demo-babel');
  });
});
